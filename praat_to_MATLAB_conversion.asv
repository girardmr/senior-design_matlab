%Gordon lab Praat values
smoothing_factor = 25;
low = 500;
high = 2500; %low and high data values?

%cd()...changes the directory;

%list the audio files in the folder
num_files = size(dir); %returns the number of audio files...
...actually returns the total number of files in folder. so folder can only contain the desired audio files
    



